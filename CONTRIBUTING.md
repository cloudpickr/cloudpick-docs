# 기여 가이드

CloudPick에 기여해 주셔서 감사합니다. 이 문서는 문서 작성 시 따라야 할 구조와 패턴을 안내합니다.

## 기여 전 확인

- 기존 문서의 구조와 톤을 우선합니다. 새 형식을 만들기보다 가까운 주제의 문서를 참고합니다.
- 기술 사실은 벤더 공식 문서, 표준 문서, 신뢰할 수 있는 재단 문서를 근거로 합니다.
- 가격, 리전, SLA, Preview/GA 여부처럼 바뀌기 쉬운 정보는 최신 공식 문서를 확인한 뒤 작성합니다.
- 문서 수정 범위를 작게 유지합니다.

## 콘텐츠 원본 (SOT)

- **`src/content/docs/ko/`가 원본**입니다. 한국어 문서를 먼저 작성·수정합니다.
- `en/`과 `ja/`는 번역본입니다. **ko에 있는 문서는 en·ja도 동일하게 완전 번역해 유지합니다.** ko 문서를 추가·수정하면 같은 PR에서 en·ja도 함께 갱신합니다.
- 3개 로케일의 파일 구조(파일명·디렉토리)는 항상 대칭을 유지합니다. ko에만 존재하고 en·ja에 없는 파일(또는 그 반대)이 생기지 않도록 합니다.

## 문서 구조 템플릿

모든 본문 문서는 가능하면 다음 순서를 따릅니다:

```
1. frontmatter (title, description)
2. 문서 기준 마커 (ko `> 문서 기준: 2026년 8월` / en `> Last reviewed: August 2026` / ja `> 文書基準: 2026年8月`)
3. ## 개요 (또는 'X란 무엇인가')
4. 본문 (벤더 비교, 핵심 개념 등)
5. ## 자주 하는 실수 — 해당되는 경우
6. ## 체크리스트 — 해당되는 경우
7. ## 관련 문서 (내부 링크)
8. ## 참고하기 (외부 공식 문서 링크)
```

**필수 항목:**
- frontmatter `title`과 `description`
- 문서 기준 마커 — ko: `> 문서 기준: 2026년 8월`, en: `> Last reviewed: August 2026`, ja: `> 文書基準: 2026年8月` (랜딩·웰컴 페이지 `index.mdx`·`introduction.mdx`는 예외)
- `## 참고하기`

## 개요 섹션 작성 가이드

- `## 개요`를 기본으로 하되, 독자의 흥미를 끌 수 있는 질문형 제목(예: `## FinOps란?`)도 허용합니다.
- 복잡한 개념은 **Mermaid 다이어그램**을 활용하여 시각적으로 설명하는 것을 권장합니다.

### Mermaid 다이어그램 스타일

- **방향:** 위→아래(`TD`) 또는 왼→오른(`LR`).
- **노드 형태:** 질문/분기는 `{}` (다이아몬드), 결과/선택지는 `[]` (사각형).
- **복잡도:** 노드 7–10개 이내로 유지합니다.
- **특수문자:** 노드 라벨에 슬래시(`/`), 괄호(`()`, `{}`)가 들어가면 반드시 따옴표로 감쌉니다. 예: `C["3. 모델 전략<br/>(Buy/Build/Train)"]`.
- **코드펜스:** 순수 백틱(```` ```mermaid ````)만 사용하고 이스케이프된 백틱(`` \` ``)은 쓰지 않습니다.
- CI(`docs-quality.yml`의 `mermaid-lint`)가 파서를 깨뜨리는 패턴을 자동으로 잡습니다. 로컬에서 `python3 scripts/lint-mermaid.py`(자동 수정은 `--fix`)로 미리 확인할 수 있습니다.

## 보조 정보 (Starlight Aside)

주의·참고·팁 등 보조 정보는 **원시 HTML이나 위첨자 각주(`¹`, `> ¹ ...`)가 아니라 Starlight 네이티브 aside 문법**으로 작성합니다. 표 셀에 각주 기호를 달아 표 밖에서 설명하는 방식 대신, 해당 제약·주의사항을 aside 블록으로 표 근처에 명시합니다.

```markdown
:::note
핵심 개념, 보충 설명
:::

:::caution
중요 경고, 안티패턴, 주의사항
:::

:::danger
즉각적 보안 위험, 데이터 손실 가능성
:::

:::tip
성공 패턴, 권장 사항
:::
```

## 접이식 블록 (`<details>`/`<summary>`) 금지

접이식 블록은 사용하지 않습니다. 딥링크(`#앵커`)로 진입 시 내용이 감춰지고, 페이지 내 검색(Pagefind)·스크린리더에서 콘텐츠가 누락되기 때문입니다. 상세 정보는 접지 말고 소제목(`###`/`####`)이나 표로 펼쳐서 항상 노출합니다. CI(`docs-quality.yml`의 `docs-consistency-lint`)가 `<details>`/`<summary>` 사용을 자동으로 잡습니다.

## 파일 확장자 (`.md` vs `.mdx`)

- JSX 컴포넌트(`<Tabs>`, `<Card>`, `<LinkCard>` 등)나 `import`가 실제로 필요한 문서만 `.mdx`로 작성합니다.
- 순수 마크다운 문서(위 aside 커스텀 문법 `:::` 포함)는 `.md`를 사용합니다. 컴포넌트를 쓰지 않는 문서를 `.mdx`로 두지 않습니다.

## 벤더 비교 표 작성 기준

- **열 순서:** AWS → Azure → Google Cloud → OCI (시장 점유율 순)
- **링크:** 서비스명 첫 등장 시 공식 문서 링크 포함
- **수치:** 변동성 높은 수치는 "공식 문서 참고" 또는 링크로 대체
- **공백:** 해당 벤더에 대응 서비스가 없으면 `—` 사용
- **중립성:** "최고", "가장 좋음" 같은 표현 금지

## 변동성 높은 정보 작성 기준

가격, 리전 수, SLA, 한도, 모델명, 토큰 단가 등은:
- 비교 판단에 필수적인 수치만 `문서 기준` 날짜와 함께 기재하되, 공식 출처 링크를 같은 행에 포함
- 그 외에는 "공식 가격 페이지를 확인하세요" 등 링크로 안내

## 문체와 용어

- 한글 주 표기, 첫 등장 시 영문 병기. 예: `가용영역(Availability Zone)`
- 벤더명과 서비스명은 공식 최신 명칭 사용. 예: `Microsoft Entra ID`
- 추측성 표현 배제. 불확실하면 "공식 문서에서 확인 필요"로 범위를 좁힘

## 물결표(~)와 취소선

마크다운(GFM)에서 반각 물결표 `~`(U+007E) 또는 `~~`가 짝을 이루면 그 사이가 **취소선**으로 렌더링됩니다. 무심코 쓰면 문구 전체가 취소선이 되는 오렌더링이 발생합니다.

- **범위 표기**에는 반각 `~`를 쓰지 마세요. 엔대시 `–`(U+2013)를 사용합니다. 예: `수 주–수 개월`, `90–365일`
- **일본어** 문서에서 범위·물결 표기는 전각 물결표 `～`(U+FF5E)를 사용합니다. 예: `数時間～12時間`
- **취소선이 정말 필요할 때만** `~~text~~`를 쓰고, 그 문구를 `scripts/lint-strikethrough.py`의 `ALLOWLIST`에 등록합니다.
- CI(`docs-quality.yml`의 `strikethrough-lint`)가 의도치 않은 취소선을 자동으로 잡습니다. 로컬에서 `python3 scripts/lint-strikethrough.py`로 미리 확인할 수 있습니다.

## 다국어·국가 가이드 (i18n)

### 번역 티어

| 티어 | 범위 | 기대 |
|------|------|------|
| Tier 1 — Global Core | 9개 도메인 + 용어집 | ko·en·ja 대칭 |
| Tier 2 — Country Guides | korea/ us/ eu/ japan/ singapore/ | 규제 레이어 |

### 번역 동기화 정책

- 기본 locale은 **ko**이며, **ko가 SOT(Source of Truth)**입니다.
- **ko에 존재하는 모든 문서는 en·ja도 동일 내용으로 완전 번역해 유지합니다.** 로케일 간 사실·날짜·구조가 어긋나지 않아야 합니다.
- ko 문서를 추가·수정하는 PR은 **같은 PR에서 en·ja 번역도 함께 반영**합니다. 번역을 즉시 완료하기 어려우면 후속 추적 이슈를 남기되, 로케일 간 사실이 모순되는 상태로 머지하지 않습니다.
- **빈 스텁 파일은 만들지 않습니다.** 번역이 필요하면 실제 내용으로 채웁니다.
- 번역 문체와 로케일별 섹션 명칭(`참고하기` / `References` / `参考資料` 등)은 로케일 관례를 따르되, 담고 있는 **정보는 3개 로케일이 동일**해야 합니다.

### 글로벌 ↔ 국가 레이어

- 글로벌 `governance/compliance.md`, `security/network-isolation.md` 등은 **개념·공통 패턴**.
- 국가 문서는 **규제 레이어**. 첫 문단에서 글로벌 문서를 링크합니다.
- 국가 index 페이지 title은 `[국가명] 개요` / `[Country] Overview` / `[国名]概要` 패턴을 따릅니다.

## 로컬 검증

커밋 전 아래를 모두 통과시킵니다. 이 검사들은 CI에서도 실행되며, 하나라도 실패하면 머지가 차단됩니다.

```bash
# 1) 빌드 + 내부 링크 검증 (starlight-links-validator)
#    깨진 내부 링크(상대·root-relative)나 누락 파일이 있으면 빌드 실패
npm run build

# 2) 문서 일관성 — 로케일 대칭성, 문서 기준 마커, 접이식(<details>) 금지
python3 scripts/lint-docs-consistency.py

# 3) Mermaid 다이어그램 파싱 안전성
python3 scripts/lint-mermaid.py

# 4) 물결표(~) 취소선 오렌더링
python3 scripts/lint-strikethrough.py
```

- **내부 링크**는 `npm run build`의 `starlight-links-validator`가 대상 존재 여부를 검증합니다(깨진 링크 시 빌드 실패). 상대경로(`../`) 표기 자체는 허용되며, 가리키는 대상이 없을 때만 실패합니다.
- **외부 링크**는 CI(`External Link Check`, lychee + 한국 도메인은 NCP 함수)가 담당합니다. 로컬에는 lychee가 없어도 되며, CI가 검사합니다.

## 외부 링크(참고하기) 관리

- `참고하기`의 외부 공식 링크는 매 커밋(push/PR)마다 CI(`External Link Check`, lychee)가 검사합니다. 한국 도메인(`.go.kr`, `.or.kr`, `.naver.com`, `.ntruss.com` 등)은 해외 CI IP에서 차단이 잦아 lychee에서 제외하고, 한국 리전 NCP 함수(`scripts/check-korean-links.mjs`)가 전담 검증합니다.
- **깨진 링크(404/410)가 발견되면 CI가 실패합니다. 발견 즉시 해당 링크를 제거하거나 유효한 공식 URL로 대체한 뒤 다시 커밋**합니다. 깨진 링크를 남긴 채 머지하지 않습니다.
- **리다이렉트(301/302/307/308)는 유효로 허용**됩니다(벤더 문서가 로케일 부가·경로 개편으로 상시 반환). 다만 가능하면 최종 URL로 직접 링크하는 것을 권장합니다. 리다이렉트 현황은 주간 스케줄 링크 점검 리포트로 추적합니다.
- 인증이 필요한 콘솔·포털 URL(예: `console.aws.amazon.com`)은 검사 오탐을 유발하므로 `참고하기`에 넣지 말고, 불가피하면 `lychee.toml`의 `exclude`에 등록합니다.
- 링크 대체 시에도 벤더 열 순서·벤더별 그룹핑(`### AWS`/`### Azure`/`### Google Cloud`/`### OCI`) 규약을 유지합니다.

## PR 체크리스트

- [ ] 수정한 문서의 `문서 기준` 마커가 현재 내용과 맞습니다 (ko/en/ja 각 표준 문구).
- [ ] 새로 추가한 기술 사실에는 공식 문서 또는 표준 문서 근거가 있습니다.
- [ ] 변동성 높은 정보는 공식 링크 중심으로 작성했습니다.
- [ ] `npm run build`가 성공합니다 (내부 링크 검증 포함 — 깨진 내부 링크·누락 파일 없음).
- [ ] `참고하기`의 외부 링크가 모두 유효합니다 (CI `External Link Check` 통과 — 깨진 링크는 즉시 제거·대체).
- [ ] `python3 scripts/lint-docs-consistency.py` 통과 (로케일 대칭성 · 문서 기준 마커 · 접이식 `<details>` 금지).
- [ ] `python3 scripts/lint-mermaid.py` 통과 (Mermaid 파싱 안전성).
- [ ] `python3 scripts/lint-strikethrough.py` 통과 (범위 표기에 반각 `~` 대신 엔대시 `–`, 일본어는 전각 `～`).
- [ ] 보조 정보는 원시 HTML·각주가 아니라 Starlight aside(`:::note` 등)로 작성했습니다.
- [ ] 새 문서를 추가했다면 `astro.config.mjs` sidebar에 포함했습니다.
- [ ] ko 문서를 추가·수정했다면 en·ja 번역도 같은 내용으로 반영했고, 3개 로케일의 파일 구조가 대칭이며 내용(사실·날짜·구조)이 일치합니다.
- [ ] 새 카테고리·사이드바 구조 변경이라면 멀티에이전트 과반 의결(5개 중 3표 이상)을 거쳤습니다.

## 문서 리뷰 기준

대규모 갱신 시에는 빌드 통과만으로 머지하지 않습니다.

1. **구조** — frontmatter, 문서 기준, 참고하기 필수
2. **사실 정확성** — 벤더 공식·표준 대비 과장, 구식 시제, 제품명·URL 불일치
3. **교차 정합** — 깨진/중복 링크, 문서 간 모순 수치
4. **벤더 중립** — 열 순서, 근거 없는 최상급 표현
5. **변동성 정보** — 가격·리전·SLA는 공식 링크 포함
6. **독자 접근성** — 신규 섹션 도입 설명, 모호한 표현
